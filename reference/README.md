# reference/

Source / background material that isn't a spec.

- `related-projects.md` — **prior-art / competitor projects** in Lattice's space (natural-language → Manim): HyperCluster-Tech/manimator, marcelo-earth/generative-manim, animg.app. Reference + analysis only.
- `sas.pdf` / `sas.docx` — original source deck/sketch material (binary; the PRDs reference "the website from the original sketch" and the client→server→cache prototype). Kept here for provenance; not part of the build path.
- `2604.05266v1 - Copy.pdf` — reference paper (binary; currently untracked — see note).

> Directly-cloned reference **repos** (e.g. `../manimator/`) are gitignored — kept locally, not committed.
> The original sketch binaries (`sas.*`) are checked in for provenance. `related-projects.md` is the
> lightweight index of external links + local clones, so the references survive without bloating git.
